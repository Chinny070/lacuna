[project]
name = "lacuna"
version = "0.2.0"
description = "LACUNA — a reusable counterfactual performance-settlement primitive for GenLayer"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
